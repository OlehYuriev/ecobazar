import LoginScreens from "@/components/screens/AuthScreens/LoginScreens";
import { FC } from "react";

const LoginPage: FC = () => {
  return (
    <>
      <LoginScreens />
    </>
  );
};

export default LoginPage;
